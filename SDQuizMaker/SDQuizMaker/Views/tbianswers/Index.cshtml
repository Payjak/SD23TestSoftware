@model IEnumerable<SDQuizMaker.Models.tbianswer>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.tbiquestion.Question)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Text)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Correct)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.tbiquestion.Question)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Text)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Correct)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IAnswerID }) |
            @Html.ActionLink("Details", "Details", new { id=item.IAnswerID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IAnswerID })
        </td>
    </tr>
}

</table>
