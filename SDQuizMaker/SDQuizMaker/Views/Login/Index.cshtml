@model SDQuizMaker.Models.Login

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<style>
    .loginbox {
        height: 185px;
        width: 400px;
        padding: 10px;
        margin-top: 50px;
        margin-left: auto;
        margin-right: auto;
        box-shadow: 0px 3px 13px -4px;
    }

    .emailLbl {
        margin-top: 15px;
        margin-left: 20px;
    }

    .passwordLbl {
        margin-top: 15px;
        margin-left: 20px;
    }

    .button {
        align-content: center;
        text-align: center;
        margin-top: 10px;
    }

    .buttonc {
        background-color: rgba(232, 34, 34, 0.81);
        border: none;
        color: #ffffff;
        font-size: 15px;
        padding: 4px 7px 4px 7px;
        border: thin #b33b3b 1px;
        text-decoration: none;
    }

    .Login {
        width: 400px;
        margin-left: -11px;
        margin-right: -10px;
        margin-top: -12px;
        text-align: center;
        font-size: x-large;
        color: white;
        background-color: rgba(232, 34, 34, 0.81);
        box-shadow: 0px 3px 13px -4px;
    }

    .txtBox {
        margin-top: 10px;
    }
</style>
@using (Html.BeginForm())
{
    @Html.Raw(TempData["msg"])
    <div class="loginbox">
        <table>
            <tr>
                <td colspan="2">
                    <div class="Login">Login</div>
                </td>
            </tr>
            <tr>
                <td>
                    <div class="emailLbl">
                        @Html.Label("Email")
                    </div>
                </td>
                <td>
                    <div class="txtBox">
                        @Html.ValidationSummary(true)
                        @Html.TextBoxFor(m => m.Email, new { @required = "required" })

                    </div>
                </td>
            </tr>
            <tr>
                <td>
                    <div class="passwordLbl">
                        @Html.Label("Password")
                    </div>
                </td>
                <td>
                    <div class="txtBox">
                        @Html.ValidationSummary(true)
                        @Html.PasswordFor(m => m.Password, new { @required = "required" })
                    </div>
                </td>
            </tr>
            <tr>
                <td colspan="2">
                    <div class="button"><input type="submit" class="buttonc" value="Login" /></div>
                </td>
            </tr>
            <tr>
                <td>
                    @Html.ActionLink("Forgot Password?", "ForgotPassword", "Login")
                </td>
            </tr>
        </table>
    </div>
}



